# The coverage document is generated by `netwatch diagnose coverage --doc`
# and compared against the catalogue by a test. Checked out with CRLF, a
# Windows regeneration rewrites every line and the diff says nothing.
docs/diagnostic-coverage.md text eol=lf
